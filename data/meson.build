i18n.merge_file(
    input: 'stylesheet.metainfo.xml.in',
    output: meson.project_name() + '.metainfo.xml',
    po_dir: meson.source_root() / 'po',
    install_dir: get_option('datadir') / 'metainfo',
    install: true
)
