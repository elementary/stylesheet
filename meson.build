project(
    'io.elementary.stylesheet',
    version: '5.2.5'
)

install_subdir(
    'elementary',
    install_dir: join_paths(get_option('datadir'), 'themes')
)

install_subdir(
    'elementary-dark',
    install_dir: join_paths(get_option('datadir'), 'plank', 'themes')
)
